services:
  openclaw:
    build: .
    container_name: openclaw
    restart: unless-stopped
    env_file: .env
    volumes:
      - openclaw-data:/root/.openclaw
    ports:
      - "19000:19000"
volumes:
  openclaw-data:
