services:
  openclaw:
    build: .
    container_name: openclaw
    restart: unless-stopped
    env_file: .env
    network_mode: host
    volumes:
      - openclaw-data:/root/.openclaw
volumes:
  openclaw-data:
