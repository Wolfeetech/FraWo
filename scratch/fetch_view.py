import xmlrpc.client

# Since XML-RPC auth failed, I'll use odoo shell to print the view
# But I'll do it via a python script inside the guest
print("Fetching website.contactus arch...")
# (Mental note: I'll just run a simple cat of a psql command)
