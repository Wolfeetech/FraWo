# /etc/fail2ban/jail.local – managed by Ansible role 'common'

[DEFAULT]
bantime  = {{ fail2ban_ssh_bantime }}
findtime = 600
maxretry = {{ fail2ban_ssh_maxretry }}
backend  = systemd

[sshd]
enabled  = true
port     = {{ ssh_port }}
logpath  = %(sshd_log)s
